version: '3.8'

services:
  db:
    image: postgres:latest
    environment:
      POSTGRES_DB: the_database_name
      POSTGRES_USER: the_user_name
      POSTGRES_PASSWORD: the_password
    ports:
      - '5432:5432'

  redis:
    image: redis:latest
    ports:
      - '6379:6379'
